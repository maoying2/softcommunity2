# PetroSoft 知识广场 (PetroSoft Knowledge Plaza) 详细需求规格说明书

## 1. 角色定义与权限矩阵

| 角色 | 权限概述 |
| :--- | :--- |
| **标准用户 (Standard)** | 基础业务角色，可进行知识检索、学习、发起公开或私信提问、对自己提问进行关闭/删除。 |
| **领域专家 (Expert)** | 核心贡献者，拥有专家标识，回答权重高，可作为私信接收方解决复杂技术问题。 |
| **系统管理员 (Admin)** | 平台维护者，负责知识建模、提问审核、AI 模型集群调度配置及全站监控。 |

---

## 2. 详细功能说明

### 2.1 菜单页面：知识地图 (Knowledge Map)

*   **页面描述**：通过可视化拓扑网络展示勘探、开发、炼化等专业的学科知识体系。

#### **功能：关系拓扑交互**
*   **功能点**：层级钻取与焦点切换。
*   **业务逻辑**：
    1. 初始加载展示一级学科节点。
    2. 点击节点，画布以该节点为中心重新布局，展示其子知识点或关联软件、课程。
*   **约束条件**：
    *   **权限**：全角色可读。
    *   **校验**：无。
    *   **状态控制**：无。
*   **字段信息**：
    *   节点名称、学科路径、关联权重、图标 ID。

#### **功能：知识项详情查看**
*   **功能点**：侧边栏信息展示与跳转。
*   **业务逻辑**：
    1. 点击具体知识点，右侧弹出详情页。
    2. 展示相关专家推荐、关联软件列表及相关视频课程入口。
*   **约束条件**：
    *   **权限**：全角色可读。
*   **字段信息**：
    *   知识点详情描述、推荐专家名、关联软件 URL、课程跳转 ID。

---

### 2.2 菜单页面：主题交流 (Community Forum)

*   **页面描述**：专业技术互助中心，支持开放式讨论与定向私密交流。

#### **功能：发起提问/私信**
*   **功能点**：提问模式切换（公开/私信）。
*   **业务逻辑**：
    1. **公开模式**：提交后进入“待审核”状态，需管理员批准后广场可见。
    2. **私信模式**：必须选择至少一名“接收人（专家/同事）”，提交后直接进入“开放”状态，仅双方可见。
*   **约束条件**：
    *   **权限**：标准用户及专家均可发起。
    *   **校验**：标题限 50 字以内；私信模式下接收人不能为空。
    *   **状态控制**：公开问题初始为 `PENDING_APPROVAL`，私信问题初始为 `OPEN`。
*   **字段信息**：
    *   `title` (标题)、`content` (正文)、`type` (OPEN/PRIVATE)、`recipientId` (接收人 ID)、`author` (发起人)。

#### **功能：会话管理（我的提问/我的私信）**
*   **功能点**：关闭提问、删除提问、私信列表展示。
*   **业务逻辑**：
    1. **关闭操作**：问题发起人可标记问题为“已关闭”，关闭后无法再新增回复。
    2. **删除操作**：发起人可彻底移除该记录。
    3. **私信展示**：按时间倒序排列作为发件人或收件人的所有私信会话。
*   **约束条件**：
    *   **权限**：仅发起人可执行关闭/删除；私信仅参与双方可见。
    *   **状态控制**：状态字段从 `OPEN` 变更为 `CLOSED`记录。
*   **字段信息**：
    *   `status` (状态流水)、`updateTime` (最后更新时间)。

#### **功能：详情互动与评分**
*   **功能点**：回复讨论、回答评分。
*   **业务逻辑**：
    1. 用户可对任何已通过审核或私信内的回答进行追加回复。
    2. 发起人可对专家回答进行质量评分（1-10 分）。
*   **约束条件**：
    *   **权限**：私信模式下仅参与人可回；评分仅提问发起人可操作。
    *   **校验**：评分值必须为整数，范围 1-10值。
*   **字段信息**：
    *   `replyContent` (回复内容)、`rating` (分值)。

---

### 2.3 菜单页面：智能问答 (AI Assistant)

*   **页面描述**：基于多种大模型的智能技术支持助手。

#### **功能：多模型智能调度**
*   **功能点**：模型引擎一键切换。
*   **业务逻辑**：
    1. 切换模型后，后续对话将通过对应的 API Endpoint 获取结果。
    2. 系统记录当前活跃 Session 对应的模型标识。
*   **约束条件**：
    *   **校验**：前端实时拉取后端模型节点的 `status`，若为异常则禁用该模型选项。
*   **字段信息**：
    *   `modelId` (模型标识)、`providerName` (厂商名称)。

#### **功能：提示词(Prompt)治理**
*   **功能点**：系统角色 Prompt 在线编辑。
*   **业务逻辑**：
    1. 允许修改 AI 的预设指令（如：要求其以“资深地球物理学家”身份回答）。
*   **约束条件**：
    *   **权限**：全角色（建议后续版本限制为管理员/专家）。
*   **字段信息**：
    *   `systemPrompt` (系统指令文本)。

---

### 2.4 菜单页面：管理后台 (Admin Dashboard)

*   **页面描述**：系统级元数据管理与运维看板。

#### **功能：服务审计与提问审核**
*   **功能点**：提问准入审核、调度日志监控。
*   **业务逻辑**：
    1. 管理员在列表中查看 `PENDING_APPROVAL` 的问题，点击“通过”使其外网可见。
*   **约束条件**：
    *   **权限**：仅系统管理员。
*   **字段信息**：
    *   审核状态标识。

#### **功能：多模型集群治理**
*   **功能点**：LLM 服务注册、负载监控。
*   **业务逻辑**：
    1. 注册新模型：输入 API Key, Base URL 及模型名称（如 gpt-4o）。
    2. 设置权限类型：`ADMIN` 账号专供或 `PERSONAL` 限额共享。
*   **约束条件**：
    *   **权限**：仅系统管理员。
    *   **校验**：API Key 必须符合对应厂商的位宽与格式要求。
*   **字段信息**：
    *   `name` (显示名称)、`apiUrl` (端点地址)、`apiKey` (密钥)、`accountType` (管理/个人)、`status` (存活状态)。
